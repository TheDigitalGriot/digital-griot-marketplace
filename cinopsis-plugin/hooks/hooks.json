{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python --version >/dev/null 2>&1 && python -c \"import yt_dlp, flask\" 2>/dev/null && echo 'cinopsis: dependencies OK' || echo 'cinopsis: NOTE — for the slash-command path run: pip install -r ${CLAUDE_PLUGIN_ROOT}/requirements.txt (the Cowork/MCP path auto-installs its own venv, no action needed)'"
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/scripts/save-session-state.py\""
          }
        ]
      }
    ],
    "PostCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python \"${CLAUDE_PLUGIN_ROOT}/scripts/restore-session-state.py\""
          }
        ]
      }
    ]
  }
}
